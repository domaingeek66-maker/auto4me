[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn api_railway:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120"
